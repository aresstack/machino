timpsd
timpsd-sim
*-sim
# host-only test binary (make dn-props), built at the repo root
dn-probe-props
*.o
machino/tests/machino-tests.exe
outputs/
3rdparty/
toolchain/
timps-qa-*/

# raw timps-qa.sh output pointed at --out dev_notes/qa-runs/... (longrun
# investigations etc.) - same rationale as timps-qa-*/ above, just a
# different --out location that pattern doesn't cover: recordings/logs, not
# for the repo, only the written-up conclusion in dev_notes/TODO.md is.
dev_notes/qa-runs/

# logs (mpv-timps.log etc.)
*.log

# T31 OSD/rotation investigation notes - keep local, not for the repo yet
T31_OSD_*.md
docs/T31-OSD-*.md

# rotation/OSD test artifacts (logs, dmesg, grabbed frames)
osd-test-out/

# python bytecode
__pycache__/
*.pyc

# local-only: exposes room locations, private IPs, and subnets - never track
docs/camera-fleet.md

# privates Sub-Repo: Kamera-Inventar, Messreihen, alles mit Raum-/Personenbezug.
# Eigenes Git darin; siehe private/README.md
private/
